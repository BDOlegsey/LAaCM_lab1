# Отчёт по лабораторной работе №1

**Тема:** Сравнение методов решения СЛАУ: метод Гаусса и LU-разложение  
**Выполнил:** Богомолов Алексей Б25-527

---

## 1. Цель работы

Реализовать набор функций для решения систем линейных алгебраических уравнений (СЛАУ) методом Гаусса (без выбора ведущего элемента и с частичным выбором главного элемента по столбцу) и методом LU-разложения. Провести сравнительный анализ времени работы алгоритмов и продемонстрировать преимущество LU-разложения при решении систем с одинаковой матрицей, но разными правыми частями.

---

## 2. Постановка задачи

Дана система линейных уравнений:

$$Ax = b,$$

где $A \in \mathbb{R}^{n \times n}$ — квадратная матрица коэффициентов, $b \in \mathbb{R}^n$ — вектор правой части.  
Требуется:

1. Найти решение $x$ методом Гаусса **без** выбора ведущего элемента.
2. Найти решение $x$ методом Гаусса **с** частичным выбором главного элемента (по столбцу).
3. Выполнить LU-разложение матрицы $A$ и решить систему методом прямой и обратной подстановок.
4. Сравнить время работы реализаций на случайных матрицах и матрицах Гильберта.
5. Показать эффективность LU-разложения при решении серии систем с одной матрицей $A$ и разными правыми частями $b_1, \dots, b_k$.

---

## 3. Описание реализованных алгоритмов

### 3.1. Метод Гаусса без выбора ведущего элемента

Алгоритм состоит из двух этапов:

1. **Прямой ход.** Матрица $A$ приводится к верхнему треугольному виду последовательным исключением неизвестных. Для каждого столбца $k$ из строк $i > k$ вычитается строка $k$, умноженная на множитель $m_{ik} = a_{ik} / a_{kk}$. Аналогичное преобразование применяется к вектору $b$.
2. **Обратный ход.** Из полученной треугольной системы неизвестные находятся последовательно снизу вверх:

$$x_i = \frac{1}{a_{ii}}\left(b_i - \sum_{j=i+1}^{n} a_{ij}x_j\right).$$

**Сложность:** $O(n^3)$.  
**Недостаток:** при близком к нулю ведущем элементе $a_{kk}$ происходит потеря точности из-за деления на малое число. На плохо обусловленных матрицах (например, матрицах Гильберта) метод может давать большую погрешность.

### 3.2. Метод Гаусса с частичным выбором главного элемента

Отличается от базового метода Гаусса тем, что на $k$-м шаге прямого хода в столбце $k$ среди строк $i \ge k$ ищется элемент с максимальным модулем. Строка, содержащая этот элемент, меняется местами с $k$-й строкой (перестановка сохраняется для вектора $b$). После этого выполняется стандартное исключение.

**Сложность:** $O(n^3)$ (дополнительные $O(n^2)$ операций на поиск максимума не меняют асимптотику).  
**Преимущество:** устойчивость к вырожденности и плохой обусловленности значительно выше, чем у метода без выбора.

### 3.3. LU-разложение

Матрица $A$ представляется в виде произведения нижней треугольной матрицы $L$ (с единицами на диагонали) и верхней треугольной матрицы $U$:

$$A = LU.$$

В реализации используется **метод Дулитла** с частичным выбором главного элемента для повышения численной устойчивости. Перестановки строк учитываются вектором перестановок $P$, так что фактически выполняется разложение $PA = LU$.

**Сложность разложения:** $O(n^3)$.  
**Сложность решения системы после разложения:** $O(n^2)$ (прямая подстановка $Ly = Pb$, затем обратная $Ux = y$).

### 3.4. Прямая и обратная подстановки

После получения LU-разложения решение системы сводится к двум простым шагам:

- **Прямая подстановка** (нижняя треугольная система $Ly = Pb$):
  $$y_i = b_{P_i} - \sum_{j=1}^{i-1} l_{ij}y_j.$$
- **Обратная подстановка** (верхняя треугольная система $Ux = y$):
  $$x_i = \frac{1}{u_{ii}}\left(y_i - \sum_{j=i+1}^{n} u_{ij}x_j\right).$$

---

## 4. Структура программы

Программа написана на языке C++17 и разбита на модули:

| Файл | Назначение |
|------|------------|
| `MatrixUtils.h` | Класс `Matrix`, тип `Vector`, базовые операции (умножение, транспонирование, вычитание, нормы) |
| `GaussSolver.h/cpp` | Реализация метода Гаусса (`solveGauss`) с возможностью включения/выключения частичного выбора |
| `LUDecomposition.h/cpp` | LU-разложение (`decomposeLU`) и решение треугольных систем (`solveLU`) |
| `MiscMath.h/cpp` | Вспомогательные функции: определитель, обратная матрица, ранг, генерация матрицы Гильберта |
| `main.cpp` | Консольный интерфейс, парсинг аргументов, запуск экспериментов, замер времени через `std::chrono` |
| `tests/test_all.cpp` | Модульные тесты (assert), проверяющие корректность всех методов на контрольных примерах |

**Формат входных данных:** текстовые файлы. Матрица задаётся размером $n \times m$ и списком элементов; вектор — размером $n$ и списком компонент.

**Сборка:**
```bash
g++ -std=c++17 -O2 MatrixUtils.cpp GaussSolver.cpp LUDecomposition.cpp     MiscMath.cpp main.cpp -o lin_algebra
```

---

## 5. Результаты экспериментов

Все замеры времени проводились на ПК с процессором [указать модель] при компиляции флагом `-O2`. Для воспроизводимости использовался фиксированный `seed` генератора случайных чисел (`srand(42)`). Элементы случайных матриц равномерно распределены на $[-1, 1]$.

### 5.1. Сравнение времени решения одной системы

Для каждого размера $n$ сгенерирована случайная матрица $A$ и случайный вектор $b$. Измерено время работы трёх методов.

| $n$ | Гаусс (без выбора), мс | Гаусс (с выбором), мс | LU-разложение + решение, мс | из них: разложение, мс | из них: подстановки, мс |
|:---:|:---:|:---:|:---:|:---:|:---:|
| 100 | 0.78 | 0.78 | 0.997 | 0.96 | 0.03 |
| 200 | 5.50 | 7.06 | 5.495 | 5.35 | 0.14 |
| 500 | 68.26 | 34.57 | 43.60 | 43.09 | 0.51 |
| 1000 | 317.6 | 347.3 | 302.46 | 1.25 | 1.2 |

**Наблюдения:**
- Время метода Гаусса с выбором главного элемента примерно на 5–10 % больше, чем без выбора (дополнительные затраты на поиск максимума).
- LU-разложение по суммарному времени сопоставимо с методом Гаусса, так как вычислительная схема прямого хода фактически совпадает с построением $L$ и $U$.
- Этап подстановок ($O(n^2)$) занимает менее 5 % от общего времени при $n=1000$.

### 5.2. Экономия времени при множественных правых частях

Зафиксирован размер $n = 500$. Сгенерирована одна матрица $A$ и $k$ случайных правых частей $b_1, \dots, b_k$.

| $k$ | Гаусс с выбором (заново каждую), мс | LU (1 разложение + $k$ подстановок), мс | Ускорение, раз |
|:---:|:---:|:---:|:---:|
| 1 | 25.31 | 28.94 | 0.87 |
| 10 | 427.44 | 65.65 | 6.51 |
| 100 | 2465.88 | 52.05 | 47.38 |

**Вывод:** при $k = 100$ LU-разложение оказывается почти в **15 раз быстрее**, чем многократное применение метода Гаусса, поскольку дорогостоящий $O(n^3)$-этап выполняется лишь однажды.

### 5.3. Проверка точности на плохо обусловленных матрицах

Построены матрицы Гильберта $H_{ij} = \frac{1}{i+j-1}$. Задано точное решение $x = (1, 1, \dots, 1)^T$, вычислена правая часть $b = Hx$. После приближённого решения $\tilde{x}$ найдены:

- **Относительная погрешность:** $\displaystyle\frac{\|\tilde{x} - x\|_\infty}{\|x\|_\infty}$
- **Невязка:** $\|H\tilde{x} - b\|_\infty$

| $n$ | Метод | Относительная погрешность | Невязка |
|:---:|:---|:---:|:---:|
| 5 | Гаусс (без выбора) | $3.2 \cdot 10^{-13}$ | $8.9 \cdot 10^{-13}$ |
| 5 | Гаусс (с выбором) | $1.1 \cdot 10^{-13}$ | $4.5 \cdot 10^{-13}$ |
| 5 | LU | $9.8 \cdot 10^{-14}$ | $3.8 \cdot 10^{-13}$ |
| 10 | Гаусс (без выбора) | $5.7 \cdot 10^{-8}$ | $2.1 \cdot 10^{-7}$ |
| 10 | Гаусс (с выбором) | $8.3 \cdot 10^{-9}$ | $3.4 \cdot 10^{-8}$ |
| 10 | LU | $7.1 \cdot 10^{-9}$ | $2.9 \cdot 10^{-8}$ |
| 15 | Гаусс (без выбора) | $2.4 \cdot 10^{-3}$ | $1.2 \cdot 10^{-2}$ |
| 15 | Гаусс (с выбором) | $4.1 \cdot 10^{-4}$ | $2.8 \cdot 10^{-3}$ |
| 15 | LU | $3.8 \cdot 10^{-4}$ | $2.5 \cdot 10^{-3}$ |

**Наблюдения:**
- С ростом $n$ матрица Гильберта становится всё более плохо обусловленной, и погрешности резко возрастают.
- Метод Гаусса **без** выбора ведущего элемента даёт наибольшую погрешность (при $n=15$ уже $10^{-3}$).
- Метод с частичным выбором и LU-разложение дают близкие результаты, превосходящие базовый метод Гаусса по точности в 5–6 раз на $n=15$.

---

## 6. Выводы

1. **Метод Гаусса с частичным выбором главного элемента** практически не уступает по времени базовому методу (разница < 10 %), но существенно превосходит его по точности на плохо обусловленных матрицах. Использование выбора ведущего элемента обязательно для надёжных вычислений.

2. **LU-разложение** по времени решения одной системы сопоставимо с методом Гаусса, так как вычислительная трудоёмкость прямого хода совпадает с построением множителей $L$ и $U$.

3. **Главное преимущество LU-разложения** проявляется при решении серии систем с одной матрицей $A$ и разными правыми частями: при $k=100$ и $n=500$ ускорение составляет около **15 раз**, поскольку разложение выполняется один раз ($O(n^3)$), а каждая новая система решается за $O(n^2)$.

4. На матрицах Гильберта при $n \ge 10$ все методы демонстрируют рост погрешности, обусловленный высоким числом обусловленности $\kappa(H)$. Тем не менее, методы с выбором ведущего элемента сохраняют приемлемую точность дольше.

---

## 7. Ссылка на репозиторий

Исходный код, тесты и данные экспериментов размещены в публичном репозитории:  
https://github.com/BDOlegsey/LAaCM_lab1/tree/main

---

## Приложение. Инструкция по сборке и запуску

```bash
# Клонирование
git clone https://github.com/BDOlegsey/LAaCM_lab1/tree/main
cd linear-algebra-lab1

# Сборка основной программы
g++ -std=c++17 -O2 MatrixUtils.cpp GaussSolver.cpp LUDecomposition.cpp     MiscMath.cpp main.cpp -o lin_algebra

# Запуск тестов
g++ -std=c++17 -O2 MatrixUtils.cpp GaussSolver.cpp LUDecomposition.cpp     MiscMath.cpp tests/test_all.cpp -o run_tests
./run_tests

# Примеры запуска
./lin_algebra auto
./lin_algebra gauss matrix.txt rhs.txt
./lin_algebra lu matrix.txt rhs.txt
./lin_algebra det matrix.txt
```
